export enum CmsPublicationStatus {
  DRAFT = 'draft',
  SCHEDULED = 'scheduled',
  PUBLISHED = 'published',
  ARCHIVED = 'archived',
}

export enum ServiceKind {
  CATEGORY = 'category',
  SPECIALTY = 'specialty',
}

export enum PortfolioProjectType {
  CASE_STUDY = 'case_study',
  SHOWCASE = 'showcase',
}

export enum CmsContentFormat {
  HTML = 'html',
  TIPTAP = 'tiptap',
  MARKDOWN = 'markdown',
}
